package progect.repository.devicesrepository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PropsPortRepository extends JpaRepository<PropsPortRepository,Integer> {
}
