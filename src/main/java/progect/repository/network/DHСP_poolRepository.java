package progect.repository.network;

import progect.domain.network.DHСP_poolDomain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DHСP_poolRepository extends JpaRepository<DHСP_poolDomain, Integer> {
}
